{
  "name": "kanade",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run build --prefix frontend && pm2 start app.py",
    "kill": "pm2 kill",
    "stop": "npm run kill",
    "dev": "python tools/dev.py",
    "dev-linux": "sudo venv/bin/python app.py",
    "deploy": "npm run build --prefix frontend && sudo pm2 start app.py --interpreter venv/bin/python"
  },
  "workspaces": [
    "frontend"
  ]
}
